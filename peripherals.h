#include "ships.h"

void keyboardReleaseFunction(unsigned char key,int x, int y);
void keyboardFunction(unsigned char key,int x,int y);
void myMouseFunction(int x,int y);




extern ship mship;
extern double mX,mY;
extern int WINDOW_SIZEX;
extern int WINDOW_SIZEY;
